Saved. I’ll follow this workflow by default for this project:
SSH only; no username/password prompts
main protected and stable
updates is default working branch
Use feat/<short-task-name> branches → PR into updates → squash-merge → auto-delete
Promote updates to main via PR when finalized